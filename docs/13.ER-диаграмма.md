``` mermaid
erDiagram
    CHAT ||--o{ ITERATION : includes
    ITERATION ||--o{ MESSAGE : includes
    CHAT {
        session_id string
        created_at datetime
        summarization_threshold int
        sliding_window_size int
    }
    ITERATION {
        iteration_id string
        status string
    }
    MESSAGE {
        message_id string
        role string
        content string
        timestamp datetime
        token_used int
    }
```
